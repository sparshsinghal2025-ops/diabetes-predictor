# 🏥 Diabetes Risk Prediction System

An end-to-end Machine Learning pipeline and interactive web application designed to predict diabetes risk using clinical patient data. The system scales raw medical indicators, handles missing diagnostic zero-values, trains multiple classifiers, and deploys the champion model via a streamlined user interface.

🚀 **Live Demo:** [Insert your Streamlit Cloud Link Here]

---

## 📊 Dataset Overview
The predictive framework is trained on clinical diagnostic data consisting of several medical predictor variables and one target variable (`Outcome`):

*   **Pregnancies:** Number of times pregnant
*   **Glucose:** Plasma glucose concentration (2 hours in an oral glucose tolerance test)
*   **BloodPressure:** Diastolic blood pressure (mm Hg)
*   **SkinThickness:** Triceps skin fold thickness (mm)
*   **Insulin:** 2-Hour serum insulin (mu U/ml)
*   **BMI:** Body mass index (weight in kg/(height in m)^2)
*   **DiabetesPedigreeFunction:** Diabetes pedigree function (genetic score)
*   **Age:** Age in years
*   **Outcome:** Class variable (0 = Non-Diabetic, 1 = Diabetic)

---

## ⚙️ Core Architecture & Pipeline

1.  **Data Imputation:** Biologically impossible `0` values found within crucial columns (`Glucose`, `BloodPressure`, `SkinThickness`, `Insulin`, `BMI`) are dynamically converted to `NaN` and imputed using column-wise medians to prevent training bias.
2.  **Preprocessing:** Robust data transformation using `ColumnTransformer` combined with `StandardScaler` to normalize continuous chemical metrics and prevent high-value attributes from skewing distances.
3.  **Model Exploration:** Automated model benchmarking utilizing `RandomizedSearchCV` cross-validation across hyperparameter matrices for three primary architectures:
    *   *Logistic Regression*
    *   *XGBoost Classifier*
    *   *Random Forest Classifier* (Selected Winner)

---

## 📈 Model Performance Results

The algorithms achieved the following performance metrics on the stratified test set during optimization:

| Model | Test Accuracy | F1-Score | Status |
| :--- | :--- | :--- | :--- |
| **Random Forest** | **74.68%** | **0.6139** | **🏆 Chosen Winner** |
| **XGBoost** | 72.73% | 0.5625 | Deprecated |
| **Logistic Regression** | 70.78% | 0.5455 | Deprecated |

---

## 🛠️ Project File Structure

```text
├── app.py                      # Interactive Streamlit Web Application
├── predict.py                  # Single-instance backend verification script
├── DiabetesPrediction.pkl      # Serialized model, preprocessor, and scaler artifacts
├── requirements.txt            # Production environment application dependencies
└── README.md                   # Project documentation portfolio
```

---

## 💻 Local Setup Installation

Follow these steps to download and initialize the application environment on your local machine:

1. **Clone the repository:**
   ```bash
   git clone https://github.com
   cd diabetes-prediction-app
   ```

2. **Install required dependencies:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Verify backend tracking pipeline:**
   ```bash
   python predict.py
   ```

4. **Launch the local Streamlit application server:**
   ```bash
   streamlit run app.py
   ```

---

## 🛡️ Medical Disclaimer
This software development project is created strictly for educational purposes, machine learning deployment modeling verification, and structural evaluation. The output generated by this application does not constitute professional medical diagnostic determinations, formal physical evaluations, or clinical health advice. 
